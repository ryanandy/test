/**** Start of imports. If edited, may not auto-convert in the playground. ****/
var imageCollection = ee.ImageCollection("projects/sat-io/setsm-dem");
/***** End of imports. If edited, may not auto-convert in the playground. *****/
var image=ee.Image(imageCollection.first())
var imgp=image.propertyNames()
print("Number of Properties for Image",imgp.length())
print(image.get('id_no'))
for (var i = 0; i < 23; i++) {
    var inc=imgp.get(i).getInfo()
    var prop=typeof(image.get(inc).getInfo())
    print(inc+": "+prop)
}